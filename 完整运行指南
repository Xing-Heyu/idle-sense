🚀 闲置计算加速器 - 完整运行指南（本地运行）

1. 安装必备工具

 安装 Python (3.8+)
 下载地址: https://www.python.org/downloads/
# 安装 Git
# 下载地址: https://git-scm.com/downloads
# 验证安装
python --version
git
 --version

2. 克隆项目
# 克隆项目到本地
git
 clone https://github.com/your-repo/idle-sense.git
cd
 idle-sense

3. 配置环境
# 创建虚拟环境（推荐）
python -m venv venv
# 激活虚拟环境
# Windows:venv\Scripts\activate
# macOS/Linux:source venv/bin/activate

4. 安装依赖
# 安装项目依赖
pip install -r requirements.txt

# 如果没有requirements.txt，安装核心依赖
pip install fastapi uvicorn streamlit requests

5. 启动系统
# 方式1: 使用自动启动脚本（推荐）
python auto_start.py
# 方式2: 手动启动各组件
# 终端1 - 启动调度中心
python scheduler/simple_server.py
# 终端2 - 启动节点客户端  
python node/simple_client.py
# 终端3 - 启动网页界面
streamlit run web_interface.py

6. 访问网页
• 网页界面: http://localhost:8501
• 调度中心API: http://localhost:8000
🔧 常见问题解决
端口占用
# 检查端口占用
netstat -ano | findstr :8501
netstat -ano | findstr :8000

# 杀死占用进程（Windows）
taskkill /PID <进程ID> /F
依赖安装失败

# 使用国内镜像源
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
节点客户端无法启动
• 检查Python版本兼容性
• 确保所有依赖正确安装
• 检查网络连接
📋 项目结构说明
idle-sense
├── user_management/                    # 用户管理模块
│   ├── __init__.py
│   ├── models.py                      # 数据模型
│   ├── auth.py                        # 认证授权
│   ├── quota.py                       # 资源配额管理
│   └── api.py                         # API接口
├── config/
│   ├── __init__.py
│   ├── settings.py                    # 配置文件
│   └── quotas.json                    # 配额配置
├── scheduler/
│   ├── simple_server.py               # 原有调度中心
│   └── task_manager.py                # 任务管理
└── web_interface.py                   # 网页界面
🎯 快速开始
1. 克隆项目 → 安装依赖 → 运行 auto_start.py
2. 打开浏览器访问 http://localhost:8501
3. 在网页界面提交计算任务
这个指南覆盖了从零开始运行项目的完整流程，适合任何新手使用。

///////////////////////////////////////////////////////////

本地运行前要做的：
🚀 立即启动系统
方法1：使用自动启动脚本（推荐）

cd c:\
idle-sense
start_windows.bat
方法2：手动启动各个组件

# 新开命令行窗口1 - 启动调度中心
cd c:\
idle-sense
python scheduler/simple_server.py

# 新开命令行窗口2 - 启动节点客户端  
cd c:\
idle-sense
python node/windows_client.py

# 新开命令行窗口3 - 启动网页界面
cd c:\
idle-sense
streamlit run web_interface.py
📋 完整的启动流程
步骤1：启动所有服务
运行 start_windows.bat 会同时启动：
• ✅ 调度中心 (端口8000) - 任务管理和分配
• ✅ 节点客户端 - 执行计算任务
• ✅ 网页界面 (端口8501) - 用户操作界面
步骤2：等待启动完成
启动后需要等待10-30秒，让所有组件完全启动并建立连接。
步骤3：访问网页
打开浏览器访问：http://localhost:8501
🔍 如何确认系统已启动
检查方法：
1. 查看命令行窗口：应该看到3个窗口分别显示启动信息
2. 检查端口：8000和8501端口应该被占用
3. 访问测试：
◦ http://localhost:8000 - 调度中心API
◦ http://localhost:8501 - 网页界面
💡 重要提醒
系统需要先启动才能使用！ 就像服务器需要开机才能访问一样。
现在请运行 start_windows.bat，等待一会儿后就可以打开网页了！
运行命令：
cd c:\
idle-sense
start_windows.bat
等待看到"所有组件启动完成"的提示后，就可以访问 http://localhost:8501 了！
