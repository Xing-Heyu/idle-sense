# 设计决策文档

## 1. 闲置判定算法（我们的核心创新）
**问题**：如何定义“个人电脑真闲置”？
**我们的答案**：
```python
def is_truly_idle():
    # 1. 用户无操作 > 5分钟（防止短暂离开误判）
    # 2. CPU < 30%，内存 < 70%（保证系统流畅）
    # 3. 屏幕锁定或无前台应用（用户不在使用）
    # 4. 正在充电或无电池（不耗用户电量）

空闲时间 > 300,
CPU使用率 < 30,
内存使用率 < 70,
屏幕已锁定或无前台应用,
正在充电或电池耗尽
    ])
