# 设计决策文档

## 🎯 核心设计原则
1. **用户无感知**：绝不干扰电脑正常使用
2. **提供者安全第一**：任务隔离、自动清理、无持久化  
3. **最小可行方案**：先解决有无，再优化效率
4. **渐进式部署**：单机→局域网→互联网

## 🔍 闲置判定算法（我们的核心创新）

### 问题定义
如何准确判断“个人电脑真闲置”而不仅仅是“用户无操作”？

### 算法设计
```python
def is_truly_idle() -> bool:
    """五重验证的闲置判定算法"""
    # [可抄部分] 基础指标获取（psutil + 平台API）
    idle_time = get_user_idle_time()      # 用户无操作时间
    cpu_usage = get_cpu_usage()           # CPU使用率
    mem_usage = get_memory_usage()        # 内存使用率
    is_locked = is_screen_locked()        # 屏幕是否锁定
    is_charging = is_power_charging()     # 是否在充电
    
    # [我们的创新] 智能判定逻辑
    return all([
        idle_time > 300,                  # 条件1: 用户离开>5分钟
        cpu_usage < 30,                   # 条件2: CPU空闲>70%
        mem_usage < 70,                   # 条件3: 内存充足
        is_locked,                        # 条件4: 屏幕已锁定
        is_charging or has_no_battery(),  # 条件5: 不消耗用户电量
    ])
