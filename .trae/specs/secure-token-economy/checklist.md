# 代币系统安全加固检查清单

## Phase 0: 声誉系统基础

### MeritRank 声誉引擎
- [x] `MeritRankEngine` 类实现核心算法
- [x] 传递衰减函数 `transmission_decay(score, distance)` 正确实现
- [x] 连接衰减函数 `connection_decay(score, connections)` 正确实现
- [x] 周期衰减函数 `period_decay(score, periods)` 正确实现
- [x] 综合声誉计算 `calculate_reputation(feedbacks)` 正确实现
- [x] MeritRank 单元测试覆盖率 ≥ 90%

### 贡献证明系统
- [x] `ContributionProof` 数据结构定义完整
- [x] 贡献证明生成器正确生成签名
- [x] 贡献证明验证器正确验证签名
- [x] 贡献分计算公式符合设计
- [x] 贡献证明单元测试通过

### BARM 防篡改机制
- [ ] `BARMConsensus` 类实现完整
- [ ] 随机验证伙伴选择算法正确
- [ ] 分布式声誉共识计算正确
- [ ] 影响力限制机制（单节点最大 10%）生效
- [ ] BARM 单元测试通过

## Phase 1: 核心安全机制

### 加密存储
- [x] `TokenEncryption` 类实现 AES-256-GCM 加密/解密
- [x] 密钥派生使用 PBKDF2-HMAC-SHA256，迭代次数 ≥ 100,000
- [x] 数据完整性 HMAC 签名正确实现
- [x] 加密模块单元测试覆盖率 ≥ 90%

### 数据持久化
- [ ] `TokenRepository` 接口定义完整
- [ ] `EncryptedTokenRepository` 正确实现加密存储
- [ ] 数据库表结构符合设计（accounts, transactions, stakes, contributions, reputation_events）
- [ ] 数据迁移工具可用且经过测试
- [ ] 仓储层单元测试通过

### 权限系统
- [ ] `Permission` 枚举包含所有代币和声誉权限
- [ ] `ROLES` 字典正确映射新权限
- [ ] `TokenPermissionService` 正确检查权限
- [ ] `@require_token_permission` 装饰器正确工作
- [ ] 权限拒绝时抛出 `PermissionDeniedError`

### 审计日志
- [ ] `AuditAction` 枚举包含所有代币和声誉操作
- [ ] 代币操作审计记录包含前后余额
- [ ] 审计日志不可修改（追加写入）
- [ ] 异常操作检测正确触发告警
- [ ] 审计日志查询功能正常

## Phase 2: 服务层安全

### 安全服务
- [ ] `TokenSecurityService` 正确集成加密、权限、审计
- [ ] 转账操作需要 `TOKEN_TRANSFER` 权限
- [ ] 质押操作需要 `TOKEN_STAKE` 权限
- [ ] 管理员调整需要 `TOKEN_MANAGE` 权限和二次验证
- [ ] 所有操作记录完整审计日志

### TokenEconomy 重构
- [ ] `TokenEconomy` 使用加密仓储
- [ ] 所有修改操作包含权限检查
- [ ] MeritRank 声誉计算集成正确
- [ ] API 接口保持向后兼容
- [ ] 集成测试通过

### 交易签名
- [ ] `TransactionSigner` 正确生成签名
- [ ] 签名包含随机数防止重放攻击
- [ ] 签名验证正确拒绝无效交易
- [ ] 签名验证失败记录安全事件

## Phase 3: 密钥管理与备份

### 密钥管理
- [ ] `KeyManager` 正确派生密钥
- [ ] 密钥仅存储在内存中
- [ ] 密钥轮换机制可用
- [ ] 恢复密钥机制可用

### 备份恢复
- [ ] 备份文件使用加密保护
- [ ] 备份包含完整性校验
- [ ] 恢复功能正确工作
- [ ] 恢复后数据一致性验证通过

## Phase 4: 调度系统集成

### 声誉驱动调度
- [ ] 调度算法考虑节点声誉
- [ ] 声誉优先级队列正确工作
- [ ] 任务分配的声誉权重正确
- [ ] 调度集成测试通过

### 激励机制
- [ ] 基于声誉的奖励加成正确计算
- [ ] 任务失败的声誉惩罚正确执行
- [ ] 周期性声誉衰减正确执行
- [ ] 激励机制测试通过

## Phase 5: 集成验证

### 依赖注入
- [ ] 所有新组件注册到 DI 容器
- [ ] `TokenEconomyService` 使用新依赖
- [ ] 容器初始化正确

### 配置系统
- [ ] `TokenSecuritySettings` 配置类完整
- [ ] `MeritRankSettings` 配置类完整
- [ ] 配置可通过环境变量覆盖
- [ ] 默认配置安全合理

### 测试覆盖
- [x] 单元测试覆盖率 ≥ 80%
- [x] 集成测试通过
- [x] 端到端安全测试通过
- [ ] 抗女巫攻击测试通过

### 文档
- [ ] 安全配置指南完整
- [ ] API 文档更新
- [ ] 迁移指南可用

## 安全验收

### 基础安全
- [ ] 无法通过直接修改数据库更改余额
- [ ] 无权限用户无法执行代币操作
- [ ] 所有代币操作有审计记录
- [ ] 数据加密存储，无法直接读取
- [ ] 密钥丢失后可通过恢复密钥恢复
- [ ] 备份数据可正确恢复

### 抗攻击能力
- [ ] 女巫攻击无效（MeritRank 衰减机制生效）
- [ ] 共谋攻击无效（BARM 随机验证）
- [ ] 刷分行为无效（连接衰减生效）
- [ ] 历史贡献自动衰减（周期衰减生效）

### 声誉系统
- [ ] 声誉计算准确反映节点贡献
- [ ] 高声誉节点获得调度优先
- [ ] 低声誉节点任务受限
- [ ] 声誉等级正确映射

## 学术方案验证

### MeritRank 验证
- [x] 三种衰减机制正确实现
- [ ] 攻击场景测试：100 账号互刷声誉 ≈ 0
- [ ] 正常场景测试：10 真实连接声誉 > 20

### PoC 验证
- [x] 贡献证明不可伪造
- [x] 贡献分计算符合公式
- [ ] 验证节点随机选择

### BARM 验证
- [ ] 单节点影响力 ≤ 10%
- [ ] 验证伙伴随机选择
- [ ] 分布式共识取中位数
